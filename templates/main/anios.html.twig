{% extends 'base_admin.html.twig' %}


{% block title %}Años Académicos{% endblock %}


{% block body %}
<ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="{{ path('homepage') }}">Inicio</a></li>
    <li class="breadcrumb-item active"><a href="{{ path('main-anios') }}">Años Académicos</a></li>
</ol>
<div class="container-fluid">
    <div id="ui-view">
        <h1>Años Académicos</h1>
        <div class="card">
            <div class="card-header">
                <h2>Listado</h2>
            </div>
            <div class="card-body">
                <div class="row mb-2">
                    <div class="col-sm-12">
                        <a href="{{ path('main-anios-agregar') }}" class="btn btn-success">Agregar</a>
                    </div>
                </div>
                {% if lista|length > 0 %}
                    <table class="table table-responsive-sm table-bordered table-striped table-sm">
                        <thead>
                            <tr>
                                <th></th>
                                <th>Año</th>
                                <th>Vigente</th>
                                <th></th>
                            </tr>   
                        </thead>
                        <tbody>
                        {% set i = 0  %}
                        {% for item in lista %}
                            {% set  i = i + 1  %}
                            <tr>
                                <td>{{ i }}</td>
                                <td>{{ item.numero|default('') }}</td>
                                <td>{{ item.vigente ? "SI" : "NO" }}</td>
                                <td>
                                    <a href="{{ path('main-anios-editar', { anio: item.id }) }}" class="btn btn-success">Editar</a>
                                    <a href="{{ path('main-anios-eliminar', { anio: item.id }) }}" class="btn btn-danger" onclick="return confirm('¿Está seguro de eliminar este año?');">Eliminar</a>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                {% else %}
                    No hay datos.
                {% endif %} 
            </div>
        </div>

    </div>
</div>

{% endblock %}

