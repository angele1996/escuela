<!doctype html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>{% block title %}Colegio Dr. Willis Hoover K.{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="{{ asset('adminlte/plugins/fontawesome-free/css/all.min.css') }}">
    <!-- Ionicons -->
    <link rel="stylesheet" href="https://code.ionicframework.com/ionicons/2.0.1/css/ionicons.min.css">
    <!-- overlayScrollbars -->
    <link rel="stylesheet" href="{{ asset('adminlte/dist/css/adminlte.min.css') }}">
    <!-- Google Font: Source Sans Pro -->
    <link href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,400i,700" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.10/css/select2.min.css" rel="stylesheet" />
    {% block stylesheets %}{% endblock %}
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
</head>
<body class="hold-transition sidebar-mini">
    <div class="wrapper">

        <nav class="main-header navbar navbar-expand navbar-white navbar-light">
            <div class="container-fluid justify-content-between">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
                    </li>
                    {% if app.user.permisoMatricula %}
                    <li class="nav-item d-inline-block">
                        <a href="{{ path('matricula_profesor') }}" class="nav-link">Matrícula</a>
                    </li>
                    {% endif %}
                    {% if app.user.permisoBiblioteca %}
                    <li class="nav-item d-inline-block dropdown">
                        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Biblioteca</a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="{{ path('biblioteca-menu') }}">Catálogo</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('biblioteca-libro-agregar') }}">Agregar Libro</a>
                            <a class="dropdown-item" href="{{ path('biblioteca-imprimir-codigos') }}">Imprimir Códigos</a>
                            <a class="dropdown-item" href="{{ path('biblioteca-prestamos-menu') }}">Préstamos</a>
                            <a class="dropdown-item" href="{{ path('biblioteca-prestamo-masivo') }}">Préstamo Masivo</a>
                            <a class="dropdown-item" href="{{ path('biblioteca-reporte-prestamos') }}">Reporte Préstamos</a>
                            <a class="dropdown-item" href="{{ path('biblioteca-devoluciones') }}">Devoluciones</a>
                            <a class="dropdown-item" href="{{ path('biblioteca-estantes') }}">Mover de Estantes</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('biblioteca-autor-menu') }}">Autores</a>
                            <a class="dropdown-item" href="{{ path('biblioteca-editorial-menu') }}">Editoriales</a>
                        </div>
                    </li>
                    {% endif %}
                    <li class="nav-item d-inline-block">
                        <a href="{{ path('catalogo-menu') }}" class="nav-link">Catálogo Biblioteca</a>
                    </li>
                </ul>
                <a class="btn btn btn-danger" href="{{ path('app_logout') }}">Cerrar Sesión</a>
            </div>
        </nav>

        <aside class="main-sidebar sidebar-dark-primary elevation-4">
            <!-- Sidebar -->
            <div class="sidebar">
                <!-- Sidebar Menu -->
                <nav class="mt-2">
                    <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
                    <!-- Add icons to the links using the .nav-icon class
                        with font-awesome or any other icon font library -->
                    {% if app.user.permisoMatricula %}
                    <li class="nav-item has-treeview">
                        <a href="{{ path('matricula_profesor') }}" class="nav-link">
                        <i class="nav-icon fas fa-tachometer-alt"></i>
                        <p>
                            Matrícula
                        </p>
                        </a>
                    </li>
                    {% endif %}
                    
                    {% if app.user.permisoBiblioteca %}
                    <li class="nav-item has-treeview">
                        <a href="{{ path('biblioteca-menu') }}" class="nav-link">
                        <i class="nav-icon fas fa-book"></i>
                        <p>
                            Biblioteca
                        </p>
                        </a>
                    </li>
                    {% endif %}
                    <li class="nav-item has-treeview">
                        <a href="{{ path('catalogo-menu') }}" class="nav-link">
                        <i class="nav-icon fas fa-book"></i>
                        <p>
                            Catálogo Biblioteca
                        </p>
                        </a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item has-treeview">
                        <a href="{{ path('admin-usuarios') }}" class="nav-link">
                        <i class="nav-icon fas fa-users"></i>
                        <p>
                            Usuarios
                        </p>
                        </a>
                    </li>
                    <li class="nav-item has-treeview">
                        <a href="{{ path('main-anios') }}" class="nav-link">
                        <i class="nav-icon fas fa-users"></i>
                        <p>
                            Año Academico
                        </p>
                        </a>
                    </li>
                    <li class="nav-item has-treeview">
                        <a href="{{ path('main-cursos') }}" class="nav-link">
                        <i class="nav-icon fas fa-users"></i>
                        <p>
                            Cursos
                        </p>
                        </a>
                    </li>
                    <li class="nav-item has-treeview">
                        <a href="{{ path('main-profesores') }}" class="nav-link">
                        <i class="nav-icon fas fa-users"></i>
                        <p>
                            Profesores
                        </p>
                        </a>
                    </li>
                    <li class="nav-item has-treeview">
                        <a href="{{ path('main-alumnos') }}" class="nav-link">
                        <i class="nav-icon fas fa-users"></i>
                        <p>
                            Alumnos
                        </p>
                        </a>
                    </li>
                    {% endif %}
                    <li class="nav-item has-treeview">
                        <a href="{{ path('app_logout') }}" class="nav-link">
                        <i class="nav-icon fas fa-sign-out-alt"></i>
                        <p>
                            Cerrar Sesión
                        </p>
                        </a>
                    </li>
                </nav>
            </div>
        </aside>

        <div class="content-wrapper">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}
            <section class="content-header">
                <div class="container-fluid">
                    {% block header %}{% endblock %}
                </div>
            </section>
            <section class="content">
                {% block body %}{% endblock %}
            </section>
        </div>

        <footer class="main-footer">
            <ul>
                <li><a href="https://colegiohooverlacisterna.cl/">Sitio Web Colegio Hoover La Cisterna</a></li>
                <li>Teléfono: +56978044935</li>
                <li>Correo Electrónico: contacto.hoover.cl@fwh.cl</li>
            </ul>
        </footer>
    </div>

    <!-- jQuery -->
    <script src="{{ asset('adminlte/plugins/jquery/jquery.min.js') }}"></script>
    <!-- Bootstrap 4 -->
    <script src="{{ asset('adminlte/plugins/bootstrap/js/bootstrap.bundle.min.js') }}"></script>
    <!-- AdminLTE App -->
    <script src="{{ asset('adminlte/dist/js/adminlte.min.js') }}"></script>

    <script src="{{ asset('bootstrap-datepicker/dist/js/bootstrap-datepicker.min.js') }}"></script>
    <script src="{{ asset('bootstrap-datepicker/dist/locales/bootstrap-datepicker.es.min.js') }}"></script>
    <script src="{{ asset('rut/jquery.rut.min.js') }}"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.10/js/select2.min.js"></script>

    <script>
        $(document).ready(function() {
            $('input').attr('autocomplete', 'off');

            $('.select2').select2();

            $('.input-fecha').datepicker({
                language: "es"
            });

            window.setTimeout(function () { 
                $(".alert").alert('close'); 
            }, 10000);
        });    
    </script>

    {% block scripts %}{% endblock %}
  </body>
</html>